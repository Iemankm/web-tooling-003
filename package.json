{
  "devDependencies": {
    "eslint": "^8.9.0",
    "husky": "^7.0.4",
    "prettier": "^2.5.1"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "eslint index.js",
    "prepare": "husky install",
    "start": "node index.js"
  }
}
